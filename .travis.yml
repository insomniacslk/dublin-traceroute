language: cpp

script: make

os:
  - linux
  - osx

sudo: required

compiler:
  - clang
  - gcc

before_install:
  - ./.travis/before_install.sh

install:
  - ./.travis/install.sh

addons:
  apt:
    packages:
      - libpcap-dev
